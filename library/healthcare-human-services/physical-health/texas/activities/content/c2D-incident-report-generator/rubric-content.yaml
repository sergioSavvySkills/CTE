# Content for c2D — Incident-Report Generator (rubric side)
# Referenced by: library/.../texas/standards/127.403-c2D.md (ai_required.content_file)
#
# Pair this file with scripts.yaml in the same directory (TTS audio generation).
# Each incident here corresponds to one audio script in scripts.yaml (match by id).
#
# Schema — each entry in `incidents` provides:
#   id                — stable identifier (e.g., "patient-fall-bathroom-v1")
#   audio_script_id   — cross-reference to the matching entry in scripts.yaml
#   gold_form_fields:
#     who: "<primary subject, role>"
#     what: "<event description, objective>"
#     when: "<time, ordinal or clock>"
#     where: "<unit, room, bed if applicable>"
#     narrative: "<3-5 sentence factual description in chronological order>"
#   required_facts: []           # objective facts that MUST appear in the student's report (completeness)
#   banned_phrases: []           # editorializing words this incident particularly invites
#                                # ("confused", "panicked", "negligent", "obviously", "appeared fine")
#   dimension_rubric:
#     factual_vs_editorial:
#       5_example: "<report entry with zero editorializing; every claim is observation + attribution>"
#       3_example: "<report with 1-2 subtle editorializing phrases beyond the banned list>"
#       1_example: "<report heavy with inference presented as fact>"
#       guidance: "score 5 when every claim is either directly observed or attributed to the source"
#     completeness:
#       5_example: "all required_facts present, chronology intact, no extraneous content"
#       3_example: "1-2 required facts missing OR chronology scrambled"
#       1_example: "core facts missing OR narrative confuses cause and effect"
#       guidance: "focus on whether a reader could reconstruct the incident from this report alone"
#
# Target: 12 incidents covering the core categories (falls, med errors, elopement, behavioral events,
# skin integrity, code situations, family complaints, near-miss).
# Status: PLACEHOLDER — content authoring pending.

incidents: []
