# Content for c1A — Active-vs-Passive Spotter
# Referenced by: library/.../texas/standards/127.403-c1A.md (ai_required.content_file)
#
# Schema — each entry in `passages` provides:
#   id          — stable identifier (e.g., "med-error-incident-v1")
#   body_text   — 8-12 sentence incident-report-style paragraph
#   sentences   — one entry per sentence (index preserved):
#     - index: 0                                # sentence position
#       text: "The medication was administered at 14:00."
#       tag: active | passive_unjustified | passive_justified
#       justification_if_justified: "action-focused — emphasis on what happened, not who did it"
#       gold_active_rewrite: "<model active rewrite — NULL for passive_justified sentences>"
#   dimension_rubric:
#     retained_meaning:
#       5_example: "<a sentence whose active rewrite fully preserves the clinical facts>"
#       3_example: "<rewrite that keeps the gist but loses a clinical detail>"
#       1_example: "<rewrite that changes the meaning, e.g., inverts responsibility>"
#       guidance: "<how to evaluate whether the student's rewrite kept the facts intact>"
#
# Target: 8 passages × ~10 sentences (distribution: 4 active / 4 passive_unjustified / 2 passive_justified per passage).
# Status: PLACEHOLDER — content authoring pending.

passages: []
