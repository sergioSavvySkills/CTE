# Content for c1A — Rewrite This Chart Note
# Referenced by: library/.../texas/standards/127.403-c1A.md (ai_required.content_file)
#
# Schema — each entry in `notes` provides:
#   id              — stable identifier (kebab-case, e.g., "cardiac-postop-v1")
#   audience        — stated reader: peer_clinician | patient | supervisor (drives audience_fit scoring)
#   source_text     — the rambling 6-sentence progress note
#   gold_rewrite    — one canonical ≤3-sentence compression (multiple acceptable answers exist)
#   required_facts  — keywords that MUST survive in the student's rewrite (deterministic completeness)
#   banned_phrases  — vague language present in source that MUST be replaced (deterministic precision)
#   dimension_rubric — per-AI-dimension scoring definitions (audience_fit, clarity):
#     audience_fit:
#       5_example: "<sentence showing a 5/5 rewrite for this audience>"
#       3_example: "<sentence showing a 3/5 rewrite>"
#       1_example: "<sentence showing a 1/5 rewrite>"
#       guidance: "<what specifically counts as audience-appropriate for THIS audience on THIS case>"
#     clarity:
#       5_example: "..."
#       3_example: "..."
#       1_example: "..."
#       guidance: "..."
#
# Target: 12 notes total, mixed across audiences and complexity levels.
# Status: PLACEHOLDER — content authoring pending.

notes: []
